return function()
	itSKIP("should be invoked on initial mount", function() end)
	itSKIP("should be invoked when updated via props", function() end)
	itSKIP("should be invoked when updated via state", function() end)
	itSKIP("should receive defaultProps", function() end)
end